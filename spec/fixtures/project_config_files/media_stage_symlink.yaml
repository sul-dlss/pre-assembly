project_name: 'media-staging-symlink'

project_style:
  content_structure: 'media'

staging_location: 'spec/fixtures/multimedia'

staging_style: 'symlink'

processing_configuration:
  style: 'media'

stageable_discovery:
  glob: '**/*'
  regex: '^((?!_techmd.xml|.md5).)*$'
  files_only: true
