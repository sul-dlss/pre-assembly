# An example of a project for which there are multiple files per object organized into folders

project_name:         'FolderManifest'

project_style:
  content_structure:  'simple_image'

bundle_dir:           'spec/test_data/obj_dirs_images'

content_md_creation:
  style:              'default'
