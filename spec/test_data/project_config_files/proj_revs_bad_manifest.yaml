# A project which uses a manifest for object discovery, one file per object, and where the objects need to be registered.

project_style:
  content_structure:  'simple_image'
  get_druid_from:     'suri'

bundle_dir:           'spec/test_data/bundle_input_a/'
staging_dir:          'tmp'
accession_items:       ~

manifest:             'manifest_badsourceid_column.csv'
checksums_file:       'checksums.txt'
desc_md_template:     'mods_template.xml'

progress_log_file:    'tmp/progress_revs.yaml'
project_name:         'Revs'
apo_druid_id:         'druid:qv648vd4392'
set_druid_id:
  - 'druid:yt502zj0924'
  - 'druid:nt028fd5773'

validate_files:       true

publish_attr:
  publish:            'no'
  shelve:             'no'
  preserve:           'yes'

content_md_creation:
  style:              'default'

stageable_discovery:
  glob:               ~
  regex:              ~

manifest_cols:
  object_container:   'filename'
  source_id:          'sourceid'
  label:              'label'

content_exclusion:    ~
