#! /usr/bin/env ruby

require File.expand_path(File.dirname(__FILE__) + '/../config/boot')

abort "Usage: #{$0} BUNDLE_DIR\n" unless ARGV.size == 1

bundle = Assembly::Bundle.new ARGV[0]
bundle.cleanup = true
bundle.run_assembly
