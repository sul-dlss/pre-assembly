#! /usr/bin/env ruby

require File.expand_path(File.dirname(__FILE__) + '/../config/boot')

abort "Usage: #{$0} MANIFEST EXP_CHECKSUMS\n" unless ARGV.size == 2
manifest, exp_checksums = ARGV

bundle = Assembly::RevsBundle.new(manifest, exp_checksums)
bundle.run_assembly
